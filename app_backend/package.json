{
  "name": "request_bin_backend",
  "version": "1.0.0",
  "description": "backend server",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../app_frontend && npm run build && cp -r dist ../app_backend"
  },
  "author": "team 1",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-promise-router": "^4.1.1",
    "mongoose": "^8.6.2",
    "nodemon": "^3.1.4",
    "pg": "^8.12.0",
    "socket.io": "^4.7.5"
  }
}
